# 探索模式 — 快捷时间跳转 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在探索模式的稳定绕飞阶段，右上角时间倍率面板的日期/时间变为可点击，展开快捷跳转面板

**Architecture:** TimePanel 根据 `orbitingBodyId && thrustMagnitude === 0` 条件判断是否可点击。点击后渲染 TimeJumpPanel 浮层组件。用户在面板中选择步长（预设或自定义），调用 store 的 `timeJump` action → 调用 engine 的 `jumpSpaceshipState` → 更新 store 的 simulatedTime/position/velocity/direction。下一帧渲染自动使用新时间计算天体位置。

**Tech Stack:** React 19 + TypeScript + Zustand 5

---

### Task 1: Add `timeJump` action to spaceshipStore

**Files:**
- Modify: `src/stores/spaceshipStore.ts`

**Purpose:** 新增一个 store action，包装 engine 层的 `jumpSpaceshipState` 调用，使 UI 层可以通过 store 完成整个跳转流程。

- [ ] **Step 1: Add `timeJump` method to `SpaceshipStore` interface**

在 `SpaceshipStore` 接口的 action 列表末尾（约第 60 行 `setWindowReady` 之后）添加：

```typescript
  timeJump: (targetTime: number) => void;
```

- [ ] **Step 2: Import `jumpSpaceshipState` from engine**

在文件顶部 import 区域（约第 6 行之后）添加：

```typescript
import { jumpSpaceshipState } from '../engine/timeJump';
```

- [ ] **Step 3: Implement `timeJump` in the store creator**

在 `create<SpaceshipStore>((set) => ({` 内，约第 330 行 `replanNavigation` 之后添加：

```typescript
  timeJump: (targetTime) => {
    const s = useSpaceshipStore.getState();
    if (!s.orbitingBodyId) return;
    const newShip = jumpSpaceshipState(
      { position: s.position, velocity: s.velocity, direction: s.direction, thrust: s.thrust, thrustMagnitude: s.thrustMagnitude, exploded: s.exploded },
      s.orbitingBodyId,
      s.simulatedTime,
      targetTime,
    );
    set({
      position: newShip.position,
      velocity: newShip.velocity,
      direction: newShip.direction,
      simulatedTime: targetTime,
    });
  },
```

- [ ] **Step 4: Run build check**

```bash
npm run build
```

Expected: PASS（无类型错误）

- [ ] **Step 5: Commit**

```bash
git add src/stores/spaceshipStore.ts
git commit -m "feat: add timeJump action to spaceshipStore"
```

---

### Task 2: Create TimeJumpPanel component and styles

**Files:**
- Create: `src/components/explore/TimeJumpPanel.tsx`
- Create: `src/components/explore/TimeJumpPanel.css`

**Purpose:** 独立的快捷时间跳转浮层面板组件，支持预设步长按钮、自定义 N 天/N 小时输入、「今日」按钮。

- [ ] **Step 1: Create CSS file**

`src/components/explore/TimeJumpPanel.css`:

```css
.time-jump-panel {
  position: absolute;
  top: 100%;
  right: 0;
  margin-top: 8px;
  z-index: 30;
  background: rgba(5, 10, 30, 0.95);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(68, 136, 255, 0.25);
  border-radius: 8px;
  padding: 16px 18px;
  min-width: 260px;
  font-family: 'Courier New', monospace;
  user-select: none;
}

.time-jump-panel-title {
  font-size: 12px;
  color: #8af;
  margin-bottom: 10px;
}

.time-jump-row {
  display: flex;
  gap: 8px;
  margin-bottom: 8px;
  align-items: center;
}

.time-jump-btn {
  flex: 1;
  padding: 8px 0;
  background: rgba(68, 136, 255, 0.12);
  border: 1px solid rgba(68, 136, 255, 0.2);
  border-radius: 5px;
  color: #aaccee;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  cursor: pointer;
  transition: background 0.12s, border-color 0.12s;
  text-align: center;
}

.time-jump-btn:hover {
  background: rgba(68, 136, 255, 0.25);
  border-color: rgba(68, 136, 255, 0.4);
  color: #ddeeff;
}

.time-jump-divider {
  border: none;
  border-top: 1px solid rgba(68, 136, 255, 0.15);
  margin: 10px 0;
}

.time-jump-custom-label {
  font-size: 10px;
  color: #667;
  margin-bottom: 6px;
}

.time-jump-custom-row {
  display: flex;
  gap: 8px;
  align-items: center;
  margin-bottom: 8px;
}

.time-jump-custom-input {
  width: 50px;
  padding: 5px 6px;
  background: #112233;
  border: 1px solid #334;
  border-radius: 4px;
  color: #ccf;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  text-align: center;
  outline: none;
}

.time-jump-custom-input:focus {
  border-color: #4488ff;
}

.time-jump-custom-text {
  font-size: 12px;
  color: #889;
  flex-shrink: 0;
}

.time-jump-custom-go {
  padding: 5px 12px;
  background: rgba(68, 136, 255, 0.15);
  border: 1px solid rgba(68, 136, 255, 0.25);
  border-radius: 4px;
  color: #aaccee;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  cursor: pointer;
  margin-left: auto;
}

.time-jump-custom-go:hover {
  background: rgba(68, 136, 255, 0.3);
  color: #ddeeff;
}

.time-jump-today {
  width: 100%;
  padding: 8px 0;
  background: #4488ff;
  border: none;
  border-radius: 5px;
  color: #fff;
  font-size: 13px;
  font-weight: 600;
  font-family: 'Courier New', monospace;
  cursor: pointer;
  margin-top: 4px;
}

.time-jump-today:hover {
  background: #5599ff;
}

.time-jump-close {
  display: block;
  text-align: center;
  font-size: 10px;
  color: #445566;
  margin-top: 8px;
  cursor: pointer;
}

.time-jump-close:hover {
  color: #8899bb;
}
```

- [ ] **Step 2: Create TSX component**

`src/components/explore/TimeJumpPanel.tsx`:

```tsx
import { useState, useCallback, useEffect, useRef } from 'react';
import { useSpaceshipStore } from '../../stores/spaceshipStore';
import './TimeJumpPanel.css';

const YEAR_MS = 365.25 * 24 * 3600 * 1000;
const MONTH_MS = 30.44 * 24 * 3600 * 1000;
const WEEK_MS = 7 * 24 * 3600 * 1000;
const DAY_MS = 24 * 3600 * 1000;
const HOUR_MS = 3600 * 1000;
const MIN10_MS = 10 * 60 * 1000;

interface Props {
  onClose: () => void;
}

export default function TimeJumpPanel({ onClose }: Props) {
  const simulatedTime = useSpaceshipStore(s => s.simulatedTime);
  const timeJump = useSpaceshipStore(s => s.timeJump);
  const [days, setDays] = useState(1);
  const [hours, setHours] = useState(1);
  const panelRef = useRef<HTMLDivElement>(null);

  const doJump = useCallback((offsetMs: number) => {
    timeJump(simulatedTime + offsetMs);
    onClose();
  }, [simulatedTime, timeJump, onClose]);

  const jumpToday = useCallback(() => {
    timeJump(Date.now());
    onClose();
  }, [timeJump, onClose]);

  // Close on outside click
  useEffect(() => {
    const handler = (e: MouseEvent) => {
      if (panelRef.current && !panelRef.current.contains(e.target as Node)) {
        onClose();
      }
    };
    document.addEventListener('mousedown', handler);
    return () => document.removeEventListener('mousedown', handler);
  }, [onClose]);

  // Close on Escape
  useEffect(() => {
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    document.addEventListener('keydown', handler);
    return () => document.removeEventListener('keydown', handler);
  }, [onClose]);

  return (
    <div className="time-jump-panel" ref={panelRef}>
      <div className="time-jump-panel-title">时间跳转</div>

      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-YEAR_MS)}>← 1年</button>
        <button className="time-jump-btn" onClick={() => doJump(YEAR_MS)}>1年 →</button>
      </div>
      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-MONTH_MS)}>← 1月</button>
        <button className="time-jump-btn" onClick={() => doJump(MONTH_MS)}>1月 →</button>
      </div>
      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-WEEK_MS)}>← 1周</button>
        <button className="time-jump-btn" onClick={() => doJump(WEEK_MS)}>1周 →</button>
      </div>
      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-DAY_MS)}>← 1天</button>
        <button className="time-jump-btn" onClick={() => doJump(DAY_MS)}>1天 →</button>
      </div>
      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-HOUR_MS)}>← 1时</button>
        <button className="time-jump-btn" onClick={() => doJump(HOUR_MS)}>1时 →</button>
      </div>
      <div className="time-jump-row">
        <button className="time-jump-btn" onClick={() => doJump(-MIN10_MS)}>← 10分</button>
        <button className="time-jump-btn" onClick={() => doJump(MIN10_MS)}>10分 →</button>
      </div>

      <hr className="time-jump-divider" />
      <div className="time-jump-custom-label">自定义</div>

      <div className="time-jump-custom-row">
        <input
          type="number"
          className="time-jump-custom-input"
          min={1}
          max={365}
          value={days}
          onChange={e => setDays(Math.max(1, Math.min(365, parseInt(e.target.value) || 1)))}
        />
        <span className="time-jump-custom-text">天后</span>
        <button className="time-jump-custom-go" onClick={() => doJump(days * DAY_MS)}>→</button>
        <button className="time-jump-custom-go" onClick={() => doJump(-days * DAY_MS)}>←</button>
      </div>
      <div className="time-jump-custom-row">
        <input
          type="number"
          className="time-jump-custom-input"
          min={1}
          max={8760}
          value={hours}
          onChange={e => setHours(Math.max(1, Math.min(8760, parseInt(e.target.value) || 1)))}
        />
        <span className="time-jump-custom-text">小时后</span>
        <button className="time-jump-custom-go" onClick={() => doJump(hours * HOUR_MS)}>→</button>
        <button className="time-jump-custom-go" onClick={() => doJump(-hours * HOUR_MS)}>←</button>
      </div>

      <button className="time-jump-today" onClick={jumpToday}>今日</button>
      <span className="time-jump-close" onClick={onClose}>面板外点击即关闭</span>
    </div>
  );
}
```

- [ ] **Step 3: Run build check**

```bash
npm run build
```

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add src/components/explore/TimeJumpPanel.tsx src/components/explore/TimeJumpPanel.css
git commit -m "feat: add TimeJumpPanel component with quick time jump shortcuts"
```

---

### Task 3: Modify TimePanel to integrate clickable date/time + popover

**Files:**
- Modify: `src/components/explore/TimePanel.tsx`

**Purpose:** 在 TimePanel 中添加条件判断（orbitingBodyId && thrustMagnitude === 0），条件满足时日期/时间区域可点击，点击后弹出 TimeJumpPanel。

- [ ] **Step 1: Add imports and state**

在 `src/components/explore/TimePanel.tsx` 顶部，将现有 import 块替换为：

```tsx
import { useCallback, useState } from 'react';
import { useSpaceshipStore } from '../../stores/spaceshipStore';
import { useExploreStore } from '../../stores/exploreStore';
import TimeJumpPanel from './TimeJumpPanel';
import './TimePanel.css';
```

- [ ] **Step 2: Add store selectors and panel state**

在 `TimePanel` 函数体内，在现有 `useExploreStore` 行之后（约第 49 行）添加：

```tsx
  const orbitingBodyId = useSpaceshipStore(s => s.orbitingBodyId);
  const thrustMagnitude = useSpaceshipStore(s => s.thrustMagnitude);
  const [showJumpPanel, setShowJumpPanel] = useState(false);

  const canTimeJump = orbitingBodyId !== null && thrustMagnitude === 0;
```

- [ ] **Step 3: Modify JSX to make date/time clickable**

将 JSX 中的 `time-panel-left` div（约第 65-68 行）从：

```tsx
      <div className="time-panel-left">
        <div className="time-panel-date">{formatDate(simulatedTime)}</div>
        <div className="time-panel-time">{formatTime(simulatedTime)}</div>
      </div>
```

替换为：

```tsx
      <div
        className={`time-panel-left${canTimeJump ? ' time-panel-left-clickable' : ''}`}
        onClick={canTimeJump ? () => setShowJumpPanel(v => !v) : undefined}
        title={canTimeJump ? '点击修改日期/时间' : undefined}
      >
        <div className="time-panel-date">{formatDate(simulatedTime)}</div>
        <div className="time-panel-time">{formatTime(simulatedTime)}</div>
      </div>
```

- [ ] **Step 4: Add TimeJumpPanel render**

在 `</div>` 关闭标签（约第 97 行）之前、即最外层 `.time-panel` div 内部末尾添加：

```tsx
      {showJumpPanel && canTimeJump && (
        <TimeJumpPanel onClose={() => setShowJumpPanel(false)} />
      )}
```

注意需要将该代码放在 `time-panel-right` div 关闭 `</div>` 之后、最外层 `</div>` 之前，以便面板相对 TimePanel 定位。

完整 JSX 结构应该变为：

```tsx
    <div className="time-panel">
      <div className={`time-panel-left${canTimeJump ? ' time-panel-left-clickable' : ''}`}
        onClick={canTimeJump ? () => setShowJumpPanel(v => !v) : undefined}>
        ...
      </div>
      <div className="time-panel-right">
        ...
      </div>
      {showJumpPanel && canTimeJump && (
        <TimeJumpPanel onClose={() => setShowJumpPanel(false)} />
      )}
    </div>
```

- [ ] **Step 5: Run build check**

```bash
npm run build
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/components/explore/TimePanel.tsx
git commit -m "feat: integrate TimeJumpPanel into TimePanel with conditional clickable date/time"
```

---

### Task 4: Add clickable styles to TimePanel CSS

**Files:**
- Modify: `src/components/explore/TimePanel.css`

**Purpose:** 添加可点击状态的蓝色高亮样式。

- [ ] **Step 1: Add clickable styles**

在 `src/components/explore/TimePanel.css` 末尾（约第 111 行之后）追加：

```css
.time-panel-left-clickable {
  cursor: pointer;
  padding: 4px 8px;
  border-radius: 4px;
  background: rgba(68, 136, 255, 0.08);
  border: 1px solid transparent;
  transition: background 0.12s, border-color 0.12s;
}

.time-panel-left-clickable:hover {
  background: rgba(68, 136, 255, 0.15);
  border-color: rgba(68, 136, 255, 0.25);
}

.time-panel-left-clickable:hover .time-panel-date {
  color: #ccddff;
}

.time-panel-left-clickable:hover .time-panel-time {
  color: #ffffff;
}
```

- [ ] **Step 2: Run build check**

```bash
npm run build
```

Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add src/components/explore/TimePanel.css
git commit -m "feat: add clickable highlight styles for time-jump panel trigger"
```

---

### Task 5: Final verification

**Files:** All modified/created files from Tasks 1-4

**Purpose:** 确保所有改动通过 lint、typecheck、和现有测试。

- [ ] **Step 1: Run all tests**

```bash
npm test
```

Expected: 所有 146 个测试 PASS（包括 `timeJump.test.ts` 的 17 个测试）

- [ ] **Step 2: Run lint**

```bash
npm run lint
```

Expected: 新文件无 lint 错误（预存在的其他文件错误不计入）

- [ ] **Step 3: Run build**

```bash
npm run build
```

Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: final verification — all tests pass, lint clean, build succeeds"
```
